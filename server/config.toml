[server]
host = "0.0.0.0"
port = 8800

[security]
# Shared password for subscription encryption - must match client
# shared_password = "your-secure-shared-password-here"
# Admin API token
# admin_token = "admin-api-secret-token"

[database]
path = "./data/subscriptions.db"
